# ============================================================
# STOCKSENSE FRONTEND ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================
# Default: Uses relative '/api' (proxied by Vite dev server or Firebase Hosting rewrite)
# VITE_API_BASE_URL=/api

# For direct Cloud Run connection (without Firebase rewrites):
# VITE_API_BASE_URL=https://your-cloud-run-service.a.run.app/api
